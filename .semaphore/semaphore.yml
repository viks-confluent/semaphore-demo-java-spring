version: v1.0
name: Java Spring example CI pipeline on Semaphore
agent:
  machine:
    type: s1-viks-local-test
    os_image: ''
blocks:
  - name: Build
    task:
      env_vars:
        - name: MAVEN_OPTS
          value: '-Dmaven.repo.local=.m2'
      jobs:
        - name: Hello Agent!
          commands:
            - echo "Hello Agent!"
